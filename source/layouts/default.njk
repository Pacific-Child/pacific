---
title: UNICEF Pacific Regional Council for Early Childhood Development
---

<!DOCTYPE html>
<html class="no-js" lang="en">
	<head>
		<meta charset="utf-8">
		<title>{{site.title}}</title>
		<meta name="description" content="">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link rel="stylesheet" href="/stylesheets/main.css">
		<script>
			// Add a class to the html element to confirm JS is available
			// -> used by the CSS for styling JS-dependent elements
			document.documentElement.className = "js";
			// Add a class if the browser/device supports touch
			document.documentElement.className += (("ontouchstart" in document.documentElement) ? ' touch' : ' no-touch');
		</script>

	</head>
	<body
	  x-data="{ navOpen: false }"
	  :class="{ 'c-overlay-freeze': navOpen == true }"
	>
		{# Main header #}
		{% MainNav {
			logo: '/images/pacific-logo-icon.svg',
			items: site.header.nav,
			currentPage: page.url
		} %}

		{# Content #}
		<main>{{ content | safe }}</main>

		{# Main footer #}
		{% MainFooter {
			logo: '/images/pacific-logo-icon.svg',
			title: site.title,
			menu: site.header.nav,
			currentPage: page.url,
			strings: site.footer
		} %}

		{# Alpine JS from their CDN #}
		<script
			type="module"
			src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js"
		></script>
		<script src="https://cdn.jsdelivr.net/gh/alpine-collective/alpine-magic-helpers@0.5.x/dist/truncate.min.js"></script>
		<script
			nomodule
			src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/
			alpine-ie11.min.js"
			defer
		></script>
	</body>
</html>
